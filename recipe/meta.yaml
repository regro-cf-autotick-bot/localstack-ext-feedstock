{% set name = "localstack-ext" %}
{% set version = "0.10.107" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz"
  sha256: 829fcdbe00ccafa649f0d02dad622d9551f1ab841ebdfd31c7e6480fa43fcdc6

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . -vv"

requirements:
  host:
    - pip
    - python
  run:
    - dnslib
    - dnspython
    - pyaes
    - python

test:
  imports:
    - localstack_ext
    - localstack_ext.bootstrap

about:
  home: "https://github.com/localstack/localstack"
  license: "Apache-2.0"
  license_family: "APACHE"
  license_file: "LICENSE.txt"
  summary: "Extensions for LocalStack"
  doc_url: "https://pypi.org/project/localstack-ext/"
  dev_url: "https://github.com/localstack/localstack"

extra:
  recipe-maintainers:
    - mwilson8
    - oblute
    - benhuff
